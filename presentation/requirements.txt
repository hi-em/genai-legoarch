# Build the deck:  python presentation/build_deck.py
# Outputs legoarch-deck.pdf + legoarch-deck.pptx (rendered at 2x / 3840x2160).
Pillow
python-pptx
reportlab
svglib          # rasterise Lucide SVG icons
rlPyCairo       # cairo backend for svglib/renderPM
pycairo
fonttools       # per-glyph font fallback (Nunito is missing the arrow glyph)
# Fonts (in presentation/fonts/): Nunito.ttf, DMSans.ttf  (Google Fonts, OFL)
# Icons (in presentation/_icons/): Lucide static SVGs (ISC)
